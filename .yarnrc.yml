nodeLinker: node-modules

# Yarn Classic always ran dependency install/postinstall scripts (e.g. swiftlint's
# binary download, native module builds). Keep that behavior under Berry.
enableScripts: true
